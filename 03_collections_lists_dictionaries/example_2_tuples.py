# Topic: Tuples
# Description: Tuples store multiple items like lists, but cannot be changed.
# Key idea: Tuples are ordered but immutable - not changable.

coordinates = (10, 20)

print("Coordinates:", coordinates)
print("X value:", coordinates[0])

# This would cause an error:
# coordinates[0] = 15
